<form method="post" action="{{ path('app_caisse_tarif_lettre_suivie_delete', {'id': tarif_lettre_suivie.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce tarif ?');" class="d-inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarif_lettre_suivie.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
