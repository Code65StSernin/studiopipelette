{% extends 'caisse/layout.html.twig' %}

{% block title %}Dépenses{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">Dépenses</h1>
        <a href="{{ path('app_caisse_depense_new') }}" class="btn btn-primary">
            <i class="fas fa-plus"></i> Nouvelle dépense
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="bg-light">
                    <tr>
                        <th>Date</th>
                        <th>Objet</th>
                        <th>Montant</th>
                        <th>Pointage</th>
                        <th>Remb. Anticipé</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for depense in depenses %}
                    <tr>
                        <td class="align-middle">{{ depense.date ? depense.date|date('d/m/Y') : '' }}</td>
                        <td class="align-middle">{{ depense.objet }}</td>
                        <td class="align-middle">{{ depense.montant|number_format(2, ',', ' ') }} €</td>
                        <td class="align-middle">
                            {% if depense.pointage %}
                                <span class="badge bg-success">Oui</span>
                            {% else %}
                                <span class="badge bg-secondary">Non</span>
                            {% endif %}
                        </td>
                        <td class="align-middle">
                            {% if depense.remboursementAnticipe %}
                                <span class="badge bg-info">Oui</span>
                            {% else %}
                                <span class="badge bg-secondary">Non</span>
                            {% endif %}
                        </td>
                        <td class="align-middle text-end">
                            <a href="{{ path('app_caisse_depense_edit', {'id': depense.id}) }}" class="btn btn-sm btn-outline-primary me-1">
                                <i class="fas fa-edit"></i>
                            </a>
                            <form method="post" action="{{ path('app_caisse_depense_delete', {'id': depense.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette dépense ?');" style="display: inline-block;">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depense.id) }}">
                                <button class="btn btn-sm btn-outline-danger">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6" class="text-center py-4">Aucune dépense trouvée</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    
    <div class="mt-3">
        {{ knp_pagination_render(depenses) }}
    </div>
{% endblock %}
