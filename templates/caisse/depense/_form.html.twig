{{ form_start(form) }}
    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_row(form.date) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.montant) }}
        </div>
        <div class="col-md-12 mb-3">
            {{ form_row(form.objet) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.pointage) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.remboursementAnticipe) }}
        </div>
    </div>
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
