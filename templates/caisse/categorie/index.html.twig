{% extends 'caisse/layout.html.twig' %}

{% block title %}Catégories Caisse{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Catégories Caisse (Paramètres)</h1>
        <a href="{{ path('categorie_vente_new') }}" class="btn btn-primary">
            <i class="fas fa-plus"></i> Nouvelle catégorie
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <table class="table table-hover mb-0">
                <thead class="bg-light">
                    <tr>
                        <th>Nom</th>
                        <th>Type</th>
                        <th>Visuel</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for categorie in categorie_ventes %}
                    <tr>
                        <td class="align-middle fw-bold">{{ categorie.nom }}</td>
                        <td class="align-middle">
                            {% if categorie.prestation %}
                                <span class="badge bg-info text-dark">Prestation</span>
                            {% else %}
                                <span class="badge bg-success">Vente</span>
                            {% endif %}
                        </td>
                        <td class="align-middle">
                            {% if categorie.image %}
                                <img src="{{ asset('assets/categories/' ~ categorie.image) }}" alt="{{ categorie.nom }}" style="width: 30px; height: 30px; object-fit: cover; border-radius: 4px;">
                            {% elseif categorie.couleur %}
                                <div style="width: 30px; height: 30px; background-color: {{ categorie.couleur }}; border-radius: 4px; border: 1px solid #ddd;"></div>
                            {% else %}
                                <span class="text-muted small">Aucun</span>
                            {% endif %}
                        </td>
                        <td class="align-middle text-end">
                            <a href="{{ path('categorie_vente_edit', {'id': categorie.id}) }}" class="btn btn-sm btn-outline-primary me-1">
                                <i class="fas fa-edit"></i>
                            </a>
                            <form method="post" action="{{ path('categorie_vente_delete', {'id': categorie.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');" style="display: inline-block;">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
                                <button class="btn btn-sm btn-outline-danger">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center py-4">Aucune catégorie trouvée</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
